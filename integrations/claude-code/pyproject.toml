[project]
name = "engram-mcp"
version = "0.2.0"
description = "MCP server providing persistent semantic memory for Claude Code via engram"
requires-python = ">=3.12"
license = "MIT"
dependencies = [
    "mcp[cli]>=1.0.0",
    "httpx>=0.27.0",
    "pydantic-settings>=2.5.0",
]

[project.optional-dependencies]
dev = [
    "pytest>=8.0",
    "pytest-asyncio>=0.24.0",
    "respx>=0.22.0",
]

[project.scripts]
engram-mcp = "engram_mcp.server:main"
engram-inbox-wait = "engram_mcp.inbox_wait:main"
engram-doctor = "engram_mcp.doctor:main"

[build-system]
requires = ["setuptools>=75.0"]
build-backend = "setuptools.build_meta"

[tool.setuptools.packages.find]
where = ["src"]

[tool.pytest.ini_options]
asyncio_mode = "auto"
asyncio_default_fixture_loop_scope = "session"
asyncio_default_test_loop_scope = "session"
testpaths = ["tests"]
